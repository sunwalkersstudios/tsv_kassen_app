{
  "name": "tsv-kassenapp-functions",
  "description": "Cloud Functions for TSV KassenApp",
  "version": "0.1.0",
  "engines": {
    "node": "^18 || ^20"
  },
  "private": true,
  "main": "index.js",
  "scripts": {
    "lint": "echo 'no lint'",
    "build": "echo 'no build (plain JS)'",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.0",
    "googleapis": "^133.0.0"
  }
}
